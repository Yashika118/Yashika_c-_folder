// Example:  fibonacci(n)=fibonacci(n-1)+fibonacci(n-2)
//  recusion se time complexity-->(2^n)
//  DP se time complexity-->o(n)
//  DP is --> recursion + memorisation
//  DP is used only when one task occurs more than one time
//  in DP return krne se pehle value ko store krenge
//  in DP calculate krne se pehle check krenge ki iski value already store h?
//  DP--> TWO SOLTUIONS HOTE H
//  1.-->TOP DOWN APPROACH (REC + MEMORISATION )
//  2.-->BOTTOM UP APPROACH (ITERATIVE APPROACH)



//Memoization --> tend to store the value of sub problems in some map or table
//Tabulation -->(Bottom Up)--> Base Case to the Required